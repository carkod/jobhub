version: "3.1"
services:
    # front:
    #   image: carloswufei/jobhub-front
    #   restart: unless-stopped
    #   container_name: jobhub-front
    #   ports: 
    #         - 1337:80
    # back:
    #     image: carloswufei/jobhub-back
    #     restart: unless-stopped
    #     container_name: jobhub-back
    #     ports:
    #         - 9000:9000
    #     volumes:
    #         - ./api:/api
    #         - /api/node_modules
    #     depends_on:
    #         - db
    db:
      restart: unless-stopped
      image: mongo:latest
      volumes:
        - ./mongo_db:mongo_db
      ports:
        - 27017:27017
      environment:
        ME_CONFIG_MONGODB_ADMINUSERNAME: root
        ME_CONFIG_MONGODB_ADMINPASSWORD: admin
volumes:
    mongo_data:
